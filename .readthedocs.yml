version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    install:
      - pip install --upgrade pip
      - pip install . --group docs

sphinx:
  configuration: docs/conf.py
  builder: html
  fail_on_warning: true
